# cook your dish here
T=int(input())
l=[]
for i in range(T):
    a,b,c,d=map(int,input().split())
    l.extend([a,b,c,d])
    m=max(l)
    if sum(l)-m<m:
        print("YES")
    else:
        print("NO")
    l.clear()
