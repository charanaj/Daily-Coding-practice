# cook your dish here
T=int(input())
for i in range(T):
    x,y=map(int,input().split())
    l=list(map(int,input().split()))
    c=0
    for i in range(x):
        if l[i]>=y:
            c+=1
    print(c)
