# cook your dish here
T=int(input())
for i in range(T):
    a,b=map(int,input().split())
    x,y=map(int,input().split())
    if a<=x and b<=y:
        print("POSSIBLE")
    else:
        print("IMPOSSIBLE")
