# cook your dish here
import math
T=int(input())
for i in range(T):
    x=int(input())
    if x%5!=0:
        print('-1')
    else:
        tens=x//10
        fives=(x%10)//5
        if x%10==0:
            print(tens)
        else:
            print(tens+1)
    
