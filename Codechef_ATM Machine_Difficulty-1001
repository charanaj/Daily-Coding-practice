# cook your dish here
t=int(input())
for i in range(t):
    n,k=map(int,input().split())
    l=list(map(int,input().split()))
    c=''
    for i in l:
        if i <=k:
            k-=i
            c+='1'
        else:
            c+='0'
    print(c)
