# cook your dish here
import math
T=int(input())
for i in range(T):
    x,y=map(int,input().split())
    if x>y:
        a=x-y
        r=a/4
        print(math.ceil(r))
    elif x<=y:
        print('0')
