# cook your dish here
T=int(input())
a=10
b=5
for i in range(T):
    x,y=map(int,input().split())
    if a*x > b*y:
        print("FIRST")
    elif a*x < b*y:
        print("SECOND")
    else:
        print("ANY")
